{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "plugin-window",
  "description": "Capability for plugin result window",
  "windows": ["plugin-window"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
